# Internet Protocol

The fundamental protocol of the [[TCP-IP-Model]] that handles logical addressing and routing of data packets across networks.

## Key Point
IP provides the framework for moving data between devices on different networks by assigning unique addresses (IP addresses) and determining the path packets take to reach their destination.

## Main Versions
- [[IPv4]] - 32-bit addressing
- [[IPv6]] - 128-bit addressing